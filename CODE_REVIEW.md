# DevQuiz 코드 구조 및 디버깅 리포트

## 📋 목차
1. [전체 아키텍처](#전체-아키텍처)
2. [주요 플로우](#주요-플로우)
3. [파일별 상세 설명](#파일별-상세-설명)
4. [발견된 문제점 및 개선사항](#발견된-문제점-및-개선사항)
5. [타입 안정성 이슈](#타입-안정성-이슈)

---

## 전체 아키텍처

### 디렉토리 구조
```
src/
├── app/                    # Next.js App Router
│   ├── page.tsx           # 홈 페이지 (메인 진입점)
│   ├── (auth)/            # 인증 관련 라우트 그룹
│   │   └── login/         # 로그인 페이지
│   ├── (pages)/           # 실제 페이지 라우트 그룹
│   │   ├── onboarding/   # 온보딩 플로우
│   │   └── home/          # 홈 페이지 (미사용)
│   └── quiz/              # 퀴즈 관련
│       ├── actions.ts     # 서버 액션 (퀴즈 시작/제출/완료)
│       ├── page.tsx       # 퀴즈 메인 (리다이렉트)
│       ├── start/         # 퀴즈 시작 페이지
│       └── result/        # 결과 페이지
├── components/
│   ├── onboarding/        # 온보딩 컴포넌트
│   ├── quiz/              # 퀴즈 UI 컴포넌트
│   └── ui/                # Shadcn UI 컴포넌트
├── lib/
│   ├── quiz/              # 퀴즈 도메인 로직
│   │   ├── generate-daily-quiz.ts  # 퀴즈 생성 로직
│   │   └── utils.ts       # 유틸리티 함수
│   ├── validations/       # Zod 스키마
│   └── errors.ts          # 커스텀 에러 클래스
└── types/                 # TypeScript 타입 정의
```

### 기술 스택
- **프레임워크**: Next.js 16 (App Router)
- **언어**: TypeScript
- **스타일링**: Tailwind CSS v4
- **UI 컴포넌트**: Shadcn/ui
- **백엔드**: Supabase (Auth + Database)
- **검증**: Zod
- **상태 관리**: React Hooks (useState, useTransition)

---

## 주요 플로우

### 1. 사용자 인증 플로우
```
로그인 → OAuth 콜백 → 프로필 확인 → 온보딩 여부 확인
```

**파일**: `app/(auth)/auth/callback/route.ts`
- OAuth 인증 후 프로필의 `is_onboarded` 확인
- 온보딩 미완료 시 `/onboarding`으로 리다이렉트
- 완료 시 `/quiz`로 리다이렉트 (⚠️ 실제로는 `/`로 가야 함)

### 2. 온보딩 플로우
```
닉네임 입력 → 카테고리 선택 (최소 3개) → DB 저장 → 홈으로 이동
```

**주요 파일**:
- `app/(pages)/onboarding/page.tsx`: 서버 컴포넌트 (온보딩 상태 확인)
- `app/(pages)/onboarding/actions.ts`: 서버 액션 (데이터 저장)
- `components/onboarding/onboarding-client.tsx`: 클라이언트 UI

**동작**:
1. 서버에서 프로필과 카테고리 확인
2. 완료 조건: `nickname` 존재 + `user_categories` 3개 이상
3. 미완료 시 온보딩 UI 표시
4. 완료 시 `profiles.nickname`, `profiles.is_onboarded`, `user_categories` 저장

### 3. 퀴즈 플로우
```
홈 → 퀴즈 시작 → 문제 풀이 → 답변 제출 → 결과 확인
```

**주요 파일**:
- `app/page.tsx`: 홈 페이지 (통계 조회)
- `app/quiz/start/page.tsx`: 퀴즈 시작 페이지
- `app/quiz/actions.ts`: 서버 액션 (퀴즈 시작/제출/완료)
- `components/quiz/quiz-client.tsx`: 퀴즈 UI

**동작**:
1. 홈에서 "오늘의 퀴즈" 시작 버튼 클릭
2. `/quiz/start`에서 `startDailyQuizAction()` 호출
3. 오늘자 attempt 확인 → 없으면 새 퀴즈 생성
4. 문제별로 답변 제출 (`submitAnswerAction`)
5. 마지막 문제 완료 시 `completeQuizAction` 호출
6. 결과 페이지로 이동

### 4. 퀴즈 생성 로직
```
유저 카테고리 조회 → 이미 푼 문제 제외 → 난이도별 샘플링 → 셔플
```

**파일**: `lib/quiz/generate-daily-quiz.ts`

**알고리즘**:
1. 유저의 관심 카테고리 조회 (`user_categories`)
2. 이미 푼 문제 ID 목록 조회 (`quiz_answers` via `quiz_attempts`)
3. 난이도별 문제 샘플링:
   - Easy (난이도 1): 2개
   - Medium (난이도 2): 2개
   - Hard (난이도 3): 1개
4. Fisher-Yates 셔플로 순서 랜덤화
5. 문제 부족 시 fallback 로직 (난이도 무시)

---

## 파일별 상세 설명

### 핵심 서버 액션

#### `app/quiz/actions.ts`

**`startDailyQuizAction()`**
- 오늘자 attempt 확인
- 완료된 attempt 있으면 결과 페이지로 리다이렉트
- 진행 중인 attempt 있으면 해당 질문 반환
- 없으면 새 퀴즈 생성 및 attempt 생성

**`submitAnswerAction()`**
- 답변 타입별 검증 (Zod)
- 정답 비교 로직:
  - 객관식: 선택한 인덱스의 옵션과 정답 비교
  - O/X: boolean 변환 후 비교
  - 빈칸/코드: 문자열 trim 후 대소문자 무시 비교
- `quiz_answers`에 저장 (upsert)

**`completeQuizAction()`**
- 모든 답변 조회하여 점수 계산
- `quiz_attempts` 업데이트 (완료 상태)
- 오답 노트 저장 (`wrong_notes`)
- 스트릭 업데이트 (`user_streaks`)

### 퀴즈 생성 로직

#### `lib/quiz/generate-daily-quiz.ts`

**`generateDailyQuiz()`**
- 순수 함수 (DB 접근만, 상태 변경 없음)
- 유저 카테고리 기반 문제 필터링
- 이미 푼 문제 제외
- 난이도별 배분 보장
- 문제 부족 시 fallback

**주의사항**:
- `getAnsweredQuestionIds()`에서 모든 attempt를 조회하므로 성능 이슈 가능
- 대량의 attempt가 있을 경우 최적화 필요

### UI 컴포넌트

#### `components/quiz/quiz-client.tsx`
- 클라이언트 컴포넌트
- 상태 관리: `useState` (현재 인덱스, 답변, 모달)
- 서버 액션 호출: `useTransition`으로 래핑
- 진행률 표시: Progress 컴포넌트

#### `components/quiz/question-card.tsx`
- 문제 타입별 UI 분기:
  - 객관식: RadioGroup
  - O/X: 버튼 2개
  - 빈칸/코드: Input

#### `components/quiz/answer-modal.tsx`
- 정답/오답 표시
- 해설 표시
- 다음 문제 또는 결과 페이지로 이동

---

## 발견된 문제점 및 개선사항

### 🔴 Critical Issues

#### 1. **온보딩 완료 후 리다이렉트 경로 불일치**
**위치**: `app/(auth)/auth/callback/route.ts:50`
```typescript
const redirectPath = isOnboarded ? '/quiz' : '/onboarding';
```
**문제**: `/quiz`로 가지만 실제 홈은 `/`임
**해결**: `/quiz` → `/`로 변경

#### 2. **홈 페이지 통계 조회 성능 이슈**
**위치**: `app/page.tsx:38-48`
```typescript
const { data: attempts } = await supabase
  .from('quiz_attempts')
  .select('id')
  .eq('user_id', user.id);

const attemptIds = attempts?.map((a) => a.id) || [];
const { count: totalAnswers } = await supabase
  .from('quiz_answers')
  .select('*', { count: 'exact', head: true })
  .in('attempt_id', attemptIds);
```
**문제**: 
- 모든 attempt를 가져온 후 count 조회
- attempt가 많을 경우 비효율적
- `attemptIds` 배열이 매우 클 경우 쿼리 제한 초과 가능

**개선안**:
```typescript
// RPC 함수 사용 또는 직접 COUNT 쿼리
const { count: totalAnswers } = await supabase
  .from('quiz_answers')
  .select('*', { count: 'exact', head: true })
  .in('attempt_id', 
    supabase.from('quiz_attempts')
      .select('id')
      .eq('user_id', user.id)
  );
```

#### 3. **이미 푼 문제 조회 성능 이슈**
**위치**: `lib/quiz/generate-daily-quiz.ts:55-80`
**문제**: 모든 attempt를 조회한 후 모든 답변 조회
**개선안**: 
- 최근 N개 attempt만 조회
- 또는 RPC 함수로 직접 question_id 목록 조회

### 🟡 Medium Issues

#### 4. **에러 처리 UX**
**위치**: `components/quiz/quiz-client.tsx:54, 78`
```typescript
alert(error instanceof Error ? error.message : '답변 제출 중 오류가 발생했습니다');
```
**문제**: `alert()` 사용은 UX가 좋지 않음
**개선안**: Toast 컴포넌트 사용 (예: Sonner, react-hot-toast)

#### 5. **타입 단언 과다**
**위치**: 여러 파일
**문제**: `as any`, `as { ... }` 사용이 많아 타입 안정성 저하
**개선안**: 
- Supabase 타입 정의 재생성
- 명시적 타입 정의 추가

#### 6. **Redirect 에러 처리 복잡도**
**위치**: `app/quiz/start/page.tsx:52-63`
**문제**: Next.js redirect 에러를 수동으로 체크하는 로직이 복잡함
**개선안**: Next.js 15의 `redirect()`는 자동으로 처리되므로 try-catch 밖으로 분리

### 🟢 Minor Issues

#### 7. **하드코딩된 값들**
- 문제 개수: 5개 고정
- 난이도 배분: Easy 2, Medium 2, Hard 1 고정
**개선안**: 설정 파일로 분리

#### 8. **카테고리 아이콘**
**위치**: `components/quiz/quiz-home-client.tsx:103-109`
**문제**: 하드코딩된 이모지
**개선안**: DB의 `categories.icon` 사용

#### 9. **스트릭 계산 로직**
**위치**: `lib/quiz/utils.ts:49-72`
**문제**: `calculateStreak()` 함수가 사용되지 않음 (actions.ts에서 직접 계산)
**개선안**: 함수 재사용 또는 제거

---

## 타입 안정성 이슈

### 현재 문제점

1. **Supabase 타입 추론 실패**
   - `profiles` 테이블 업데이트 시 타입 에러
   - `user_categories` 삽입 시 타입 에러
   - 해결: 타입 단언 사용 (`as any`)

2. **중첩된 관계 타입**
   - `quiz_answers`에서 `questions` join 시 타입 추론 실패
   - 해결: `as any` 사용

### 개선 방안

1. **Supabase 타입 재생성**
   ```bash
   pnpm gen:types
   ```

2. **명시적 타입 정의**
   ```typescript
   type ProfileUpdate = {
     nickname?: string;
     is_onboarded?: boolean;
     updated_at?: string;
   };
   ```

3. **타입 가드 함수 사용**
   ```typescript
   function isProfile(data: unknown): data is Profile {
     return typeof data === 'object' && data !== null && 'nickname' in data;
   }
   ```

---

## 코드 품질 개선 제안

### 1. 에러 처리 개선
- 커스텀 에러 클래스 활용 (`QuizGenerationError`, `QuizSubmissionError`)
- 에러 바운더리 추가
- Toast 알림으로 변경

### 2. 성능 최적화
- 통계 조회 쿼리 최적화
- 이미 푼 문제 조회 최적화
- 페이지네이션 추가 (필요 시)

### 3. 코드 재사용성
- 공통 로직 함수화
- 타입 정의 중앙화
- 상수 분리

### 4. 테스트 추가
- 유닛 테스트: 퀴즈 생성 로직
- 통합 테스트: 서버 액션
- E2E 테스트: 전체 플로우

---

## 결론

전체적으로 **잘 구조화된 코드**입니다. 주요 강점:
- ✅ 명확한 파일 구조
- ✅ 서버/클라이언트 컴포넌트 분리
- ✅ 타입 정의 체계적
- ✅ 에러 처리 구조화

개선이 필요한 부분:
- ⚠️ 성능 최적화 (통계 조회)
- ⚠️ 타입 안정성 강화
- ⚠️ UX 개선 (에러 처리)

